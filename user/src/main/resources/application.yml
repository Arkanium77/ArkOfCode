spring:
  application:
    name: ark-user
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui/index.html
